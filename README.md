# bethero — NPB 量化投注分析引擎

專門分析日本職棒 (NPB) 讓分盤與大小分的量化投注代理人。
刻意 **不** 沿用 MLB 的得分環境與模型參數。

## 設計原則

這個專案最重要的性質不是模型多準，而是 **資料不足時不會給出建議**。

`bethero.gates.grade()` 是唯一能產生「推薦」的出口，而它要求:

| 門檻 | 條件 |
|---|---|
| 模型 EV | ≥ +4% |
| 模型 vs 市場去水後機率 | ≥ 3 個百分點 |
| 盤型與結算規則 | 已確認 |
| 預告先發 | 已確認 |
| 正式先發打線 | 已公布 |
| **賠率可覆核** | **當下、可查證的報價** |
| 資料完整度 | 軟性缺口 ≤ 2 項 |

`prices_verified` 是硬性條件: EV 是賠率的函數，賠率若無法覆核，
EV 就沒有意義。它跟軟性的 `market_prices_known`（有沒有開盤價與跨莊家
比價可參考）分開 —— 後者是「參考資訊不足」，前者是「手上的數字本身不可信」。

任何一項沒過，最好的結果是「觀察」。空的 `DataReadiness()` 不論 EV
算出多少都不可能推薦 —— 這條性質有測試守著
(`test_no_data_cannot_produce_recommendation`)。

## 模組

| 模組 | 職責 |
|---|---|
| `bethero.lines` | 亞洲盤盤口解析 (`N平` / `N±XX`) 與結算規則 |
| `bethero.board` | 看板辨識結果的資料結構，每欄帶信心度 |
| `bethero.model` | NPB 專屬得分分布模型 |
| `bethero.ev` | 去水 (比例法／次方法)、期望值、一般化 Kelly |
| `bethero.breakeven` | 所需勝率／損益兩平門檻 (不需外部資料) |
| `bethero.gates` | 推薦門檻 |
| `bethero.bankroll` | 本金、單注上限、單日曝險上限 |
| `bethero.report` | 繁體中文報告輸出 |
| `config.npb_priors` | 校準狀態 + **已知偏誤** |
| `config.calibration_2026` | 2026 當季實測值 (618 場) 與推導方法 |

## 盤口解析

看板寫成 `N`、`N平`、`N+XX`、`N-XX`。**`XX` 是結算百分比，不是小數位移**:

| 寫法 | 落在關鍵整數 N 時 |
|---|---|
| `N平` | 走盤，全額退回 |
| `N-XX` | 主方輸 XX% 本金，副方贏 XX% 賠金 |
| `N+XX` | 主方贏 XX% 賠金，副方輸 XX% 本金 |

「主方」讓分盤是 **讓分方**，大小盤是 **大分**。等效盤口為
`effective = N - s/2`（`s` 為帶號結算比例）。

```
0-50 -> 0.25   (等同標準 0/0.5 盤)
1+50 -> 0.75   (等同標準 0.5/1 盤)
1-60 -> 1.30   (標準 0.25 階梯做不出來的盤口)
1+95 -> 0.525
```

這是一個 **連續內插** 的盤口系統：標準亞洲盤只能跳 0.25，這裡可以用
`XX` 在兩個相鄰半球盤之間任意取值。因此 `bethero.lines.Line` 存的是
`(base, edge)` 而非單一數值 —— 用單一等效數值會在整數落點上算錯結算。

「一輸二贏」= 讓／受讓 1.5 分（贏 1 分算輸、贏 2 分算贏），
即 `base=1, s=-1`，見 `lines.ICHI_YU_NI_EI`。

驗算：六場的上半場／全場總分比落在 **0.526–0.557**，內部一致，
佐證解讀正確（棒球 F5 總分約為全場的 53–57%）。

## 得分模型

每隊期望得分 `lam = league_rpg × off × opp_def × park × weather × home_edge`，
得分分布用 **共享環境因子的 Poisson 混合**:

```
G ~ Gamma(k, 1/k)          # 全場共用的「今天好不好打」
R_home ~ Poisson(lam_home × G)
R_away ~ Poisson(lam_away × G)
```

積分掉 `G` 之後每隊邊際分布是負二項 (捕捉得分的過度離散)，
且兩隊得分正相關 —— 這正是球場、天氣、主審好球帶的效果。
用獨立 Poisson 會系統性低估大小分尾端。

NPB 專屬處理:

* 較低的得分環境 (單隊 ~3.85 分/場，非 MLB 的 ~4.5)
* **和局**: 延長至 12 局為止，和局有實質機率
* **主隊九局下領先就不打** —— 拆成前八局與九局下兩段條件式相加。
  用單一 lambda 打折會把主場優勢一起吃掉 (這是實作時踩到並修掉的 bug)
* 先發投球局數較長

## 校準狀態 (2026-08-13)

聯盟／球場／球隊層級的參數已用 **2026 當季 618 場實際比分** 校準完成，
數值與推導方法在 `config/calibration_2026.py`。驗算: 由逐場比分加總的
各隊得分與官方 `tmb_c`/`tmb_p` 的「得点」欄 12/12 完全相符。

| 參數 | 舊先驗 | 2026 實測 |
|---|---|---|
| 單隊每場得分 | 3.85 | **3.6019** |
| バンテリンドーム PF | 0.85 | **0.962** |
| 東京ドーム PF | 0.92 | **1.022** |
| 神宮 PF | 1.08 | **1.183** |
| みずほPayPay PF | 0.97 | **1.011** |

球場係數是用迭代比例配適把球隊強弱與球場分開解出來的，不是原始場均得分。
差別很大: みずほPayPay 原始場均 8.39 全聯盟最高，但那是軟銀打線 (off 1.36)
造成的，球場本身接近中性。

## 已知偏誤 ⚠️

`config/npb_priors.py` 的 `KNOWN_BIASES` 有完整說明。618 場全季回測的結果:

> **分差分布在 ±1 分堆積 —— 讓分盤不可用**
>
> | | 模型 | 實際 | 差 |
> |---|---|---|---|
> | P(分差 = +1) | 23.76% | 17.80% | +5.96pp |
> | P(分差 = -1) | 18.64% | 11.33% | +7.31pp |
> | P(分差 = 0) | 3.13% | 1.78% | +1.35pp |
>
> 掃描 `k ∈ [6,60] × extras_resolve ∈ [0.62,0.95]` 共 24 組，一分差比例
> 只落在 38.8%-44.4%，**沒有一組接近實測的 29.1%**。調參解決不了。
>
> 後果: 看板的 `N±XX` 正好結算在整數 N 上，而 N=0 與 N=1 就是偏誤最大的
> 兩點。6-7pp 的定價誤差遠大於 3pp 的優勢門檻 —— **讓分盤一律不定價**。

> **大小分方向已驗證可用**: 同一份回測中總分累積分布在每個關鍵整數
> 誤差 < 1pp (P(>7) 模型 40.36% vs 實際 40.45%)，卡方 9.9。
> 依預測總分分十組，最低組 5.99/6.02、最高組 8.61/8.70，回歸斜率 1.21。
> 但這是 **樣本內** 回測，是技巧的上界。

仍未校準: 投手個人層級 (樣本小、球場調整粗糙、沒有左右打對位與休息天數)。

## 使用

```bash
python3 -m pytest -q                      # 95 項測試
python3 analysis/slate_2026_08_13.py      # 產生當日報告
```

## 風控

本金 100,000 單位／0.25 Kelly／單注上限 1,000 單位／
單日曝險上限 5%／不串關。

`ev._kelly` 用數值最大化 `E[log(1 + f·R)]`，而非課本的 `(bp-q)/b` ——
因為半贏／半輸的走盤盤口不是二元賭局，套二元公式會高估注碼。
